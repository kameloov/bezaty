DROP TABLE IF EXISTS `weekly`;
CREATE TABLE IF NOT EXISTS `weekly` (
	`id`	INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
	`from_date`	TEXT,
	`to_date`	TEXT,
	`value`	REAL
);
DROP TABLE IF EXISTS `settings`;
CREATE TABLE IF NOT EXISTS `settings` (
	`balance`	REAL NOT NULL,
	`first_day`	INTEGER NOT NULL,
	`id`	INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
	`language`	INTEGER,
	`user_email`	TEXT,
	`notification`	INTEGER
);
INSERT INTO `settings` (balance,first_day,id,language,user_email,notification) VALUES (5000.0,0,1,0,NULL,0);
DROP TABLE IF EXISTS `monthly`;
CREATE TABLE IF NOT EXISTS `monthly` (
	`id`	INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
	`from_date`	TEXT,
	`to_date`	TEXT,
	`value`	REAL
);
DROP TABLE IF EXISTS `item`;
CREATE TABLE IF NOT EXISTS `item` (
	`id`	INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
	`title`	TEXT NOT NULL,
	`item_date`	TEXT NOT NULL,
	`value`	REAL NOT NULL,
	`hints`	TEXT,
	`category_id`	INTEGER NOT NULL
);
INSERT INTO `item` (id,title,item_date,value,hints,category_id) VALUES (1,'fdfds','2018-11-06',50.0,NULL,0);
INSERT INTO `item` (id,title,item_date,value,hints,category_id) VALUES (2,'hfghfhf','2018-11-06',30.0,NULL,0);
DROP TABLE IF EXISTS `income`;
CREATE TABLE IF NOT EXISTS `income` (
	`id`	INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
	`category_id`	INTEGER NOT NULL,
	`value`	REAL,
	`title`	TEXT,
	`hints`	TEXT,
	`item_date`	TEXT
);
DROP TABLE IF EXISTS `category`;
CREATE TABLE IF NOT EXISTS `category` (
	`id`	INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
	`name`	TEXT,
	`details`	TEXT,
	`icon`	INTEGER,
	`balance`	REAL NOT NULL,
	`is_expense`	INTEGER DEFAULT 1
);
